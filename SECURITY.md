# 安全规范

## 1. 禁止提交的内容

以下内容不得进入 Git、PR、Issue、聊天记录或截图：

- MySQL/Redis 地址对应的密码
- Django secret key、JWT 密钥和 access/refresh token
- DeepSeek 等第三方 API Key
- 支付宝应用私钥、公钥或证书
- 邮箱密码、服务器凭据和模型下载凭据

本地配置放在根目录 `.env`。提交前检查：

```powershell
git status --short
git diff --cached --name-only
git grep -n -I -E 'password|api.?key|private.?key|secret' -- ':!markdown'
```

命令输出中如果出现真实值，立即停止提交并轮换凭据；不要只依赖删除当前行，Git 历史中也可能已经保留。

## 2. 当前基线与历史风险

当前工作区已经将 Django、邮件和支付宝配置改为环境变量读取，新上游只应上传清理后的快照。旧 `origin` 或本地旧提交历史中仍可能存在曾经写入的敏感材料，因此：

1. 不要把旧 `main` 的完整历史直接推送到新上游。
2. 轮换已经出现在代码、聊天或日志中的真实密钥。
3. 检查其他克隆、构建产物和备份中是否仍有旧值。

`.env` 被忽略只是防止新的本地配置进入提交，不能清除已经存在于 Git 历史中的值。

## 3. 开发和生产边界

- 开发环境也不要使用生产数据库、生产支付账号或生产 API Key。
- 生产环境关闭 `DEBUG`，限制 `ALLOWED_HOSTS`、CORS、数据库账号权限和 Redis 网络来源。
- Redis 和 MySQL 不应直接暴露给不受信任的公网来源。
- API 权限必须由后端检查；特别是管理、支付、审核、数据库管理接口。
- 日志只记录定位问题所需的信息，不记录请求头中的 Authorization 或完整请求体中的密码。
- 上传文件需要限制大小、类型和存储路径；媒体处理命令必须避免把用户输入直接拼接进 shell 命令。

## 4. 问题报告

发现凭据泄露、越权、任意文件写入、命令执行或支付回调问题时，不要公开发 Issue。先通知项目负责人，说明受影响文件/接口、复现条件和临时缓解措施；凭据类问题先轮换再讨论修复。
